**《深夜惊魂：一次甲骨文 VPS 踩坑、失联与“开颅”救砖全记录》**

### 引子：平静深夜里的突然黑屏

深夜 11 点，房间里只有键盘敲击声和屏幕发出的冷光。我像往常一样折腾着托管在甲骨文云（Oracle Cloud）上的 Ubuntu 节点，刚在 3x-ui 里改完一串配置，按下“保存并重启”，终端窗口突然卡住了。

几秒钟后，命令行的光标停止了闪烁，一行冰冷的提示弹了出来：`Connection reset by peer`。

“坏了。”我心中一紧。

打开 Windows 的 PowerShell，敲下 `ping <我的IP>`——100% 丢包。
尝试重新用 SSH 密钥连接——`Connection timed out`。

我熟练地打开甲骨文云控制台，找到那台运行着 Ubuntu 20.04 的 ARM 实例。控制台上的小绿点依然幽幽地亮着：**Running（运行中）**。

后台显示它活得好好的，但现实中它却彻底“死”了。一场长达数小时的救砖与折腾，就此拉开序幕。

---

### 第一折：迷雾重重——是被墙了，还是系统崩了？

在折腾代理节点时遇到失联，第一反应往往是：**IP 被墙了？**

为了验证猜想，我打开了常用的 `ping.pe` 打算做个全球 Ping 测试。然而天不遂人愿，网页刚加载出来就弹出一个红色的 Error：`Unable to create ping streams for this client`。连测试工具都给我摆烂！

换到 ITDog 重新测试。几秒钟后，几十个检测节点陆续返回结果——看到结果的那一刻，我的心凉了半截：
**不是“国内红、国外绿”，而是“全球红”！**

全球全红意味着：这不是 GFW 的拦截，而是我的 VPS 彻底断网了、系统卡死，或者内部的 `iptables` 防火墙搞出了乌龙，把全世界的流量（包括 Ping 和 SSH）一网打尽，全部拒之门外。

---

### 第二折：连环死锁——没有密码的登录框与“发不出去”的命令

既然网络断了，常规的 SSH 已经废了，我决定使用甲骨文控制台自带的 **Cloud Shell（云终端 Serial Console）** 进行救急。

等了半分钟，控制台的黑框弹了出来。按了一下回车，屏幕上终于出现了久违的系统提示：
`ubuntu login:`

我心中大喜，敲入 `ubuntu`，按下回车。下一行弹了出来：
`Password:`

我愣住了。当初在甲骨文创建实例时，使用的是 **SSH 密钥** 认证，压根就没设置过密码！我试着直接回车，或者输入空密码，屏幕上毫无悬念地提示 `Login incorrect`。

**第一道死锁：想进系统改密码，必须先登录；但想登录，必须先有密码。**

我转而寻求甲骨文的另一个黑科技——**Run Command（运行命令）** 功能。这个工具允许管理员从控制台直接向 VPS 内部发送 Shell 脚本。我兴奋地写好清空 `iptables` 和重置密码的脚本，点击 `Create command`。

然而，几分钟过去了，命令状态一直卡在 `Accepted`，详情页里赫然写着一行警告：
`Warning: No response received yet.`

我这才反应过来：**因为 VPS 的网络栈已经崩溃，运行在 VPS 内部的 Oracle Agent 根本收不到云端发出的指令！** 

第二道死锁，再次扣紧。

---

### 第三折：误入歧途——误闯 UEFI BIOS 迷宫

“只能硬刚 GRUB 单用户模式了！”

我决定在 Cloud Shell 启动时拦截系统的引导过程，强制挂载一个 bash 终端。

我在控制台点击了“强制重启”，同时在 Cloud Shell 窗口里疯狂敲击 `Esc` 键。伴随着疯狂的击键声，屏幕一闪，出现了一个蓝白相间的菜单界面：
`Standard PC (i440FX + PIIX, 1996)`
`> Device Manager`
`> Boot Manager`
`> Boot Maintenance Manager`

“这是个啥？GRUB 呢？”

我傻眼了。原来 `Esc` 键按得太早太猛，直接把我带进了主板的 **UEFI BIOS 界面**，而不是 Linux 的引导菜单！

在这个古老的 BIOS 界面里，我小心翼翼地用方向键寻路，进入 `Boot Manager`，找到 `ubuntu` 启动项。按下回车的那一瞬间，我立即改按 **方向下键（⬇️）**——这一次，奇迹终于出现，黑底白字的 `GNU GRUB` 菜单终于被我召唤了出来！

我迅速按下 `e` 键编辑引导参数，在 `linux /boot/...` 那一行的末尾加上了 `init=/bin/bash`，按下 `F10` 启动！

几秒后，屏幕停在了 `root@instance:/#`。
我终于拿到最最高权限的 root 终端了！

在终端里，我熟练地敲下：
```bash
mount -o remount,rw /
passwd ubuntu
iptables -F
ufw disable
reboot -f
```
原以为一切到此结束，谁知重启之后……**居然还是 Ping 不通！**

---

### 第四折：终极手术——离线剥离“引导卷”与开颅修复

此时我已经折腾到了凌晨。既然软修复屡屡受阻，我决定使用 Linux 运维最后的杀手锏：**硬盘移植（开颅手术）**。

1. **卸载器官**：我在甲骨文后台，将这台坏掉 VPS 的 **Boot Volume（引导卷）** 直接从实例上卸载（Detach）了下来。
2. **连接体外循环**：我开了一台正常的备份 VPS，把坏掉的这块硬盘作为“第二块数据盘”挂载到了正常 VPS 上。
3. **开腔手术**：
   登录健康的 VPS，敲下 `lsblk`，看到了挂载进来的 `/dev/sdb1`。
   执行 `sudo mount /dev/sdb1 /mnt`。
   这一刻，坏掉 VPS 的整个文件系统完全裸露在我的面前：
   * 我直接修改了 `/mnt/home/ubuntu/.ssh/authorized_keys`，强行注入了全新的公网 SSH 公钥；
   * 我打开 `/mnt/etc/ufw/ufw.conf`，把 `ENABLED=yes` 改成了 `ENABLED=no`；
   * 我清空了 `/mnt/etc/iptables/` 下的所有残留规则；
   * 我甚至修改了 `/mnt/etc/ssh/sshd_config`，强制开启了密码登录。
4. **缝合与移植**：
   执行 `sudo umount /mnt`，在控制台把硬盘从临时 VPS 上拔下，重新缝合回原来的 VPS 作为引导卷，开机！

---

### 尾声：峰回路转，曙光终现

半分钟后，我再次打开 ITDog 进行全球 Ping 检测。

回车按下，测试点一个接一个地吐出结果：
* 德国 节点：**绿（160ms）**
* 美国 节点：**绿（180ms）**
* 日本 节点：**绿（80ms）**

**系统的网卡和防火墙彻底救活了！**

然而，当我往下滑动页面，看到中国大陆的节点时——**全红（Timeout）**。
再试了一下本地 SSH 连接，依然超时。

看到这一幕，我先是一愣，随后长舒了一口气，会心地笑了。

到了这一步，所有的谜底都已经揭开：
在我今晚最初折腾代理节点配置的那几分钟里，**这个 IP 确实已经被 GFW 准确无误地封锁了**。而后来的配置错误，又导致系统防火墙彻底锁死，造成了“全球全红”的假象，把我带偏到了九霄云外。

**真正的最后一步，反而是最轻松的：**
我在甲骨文控制台点开 VNIC 设置，点下“释放公网 IP”，随后再次点击“分配临时公网 IP”。甲骨文在几秒钟内为我生成了一个全新的 IP 地址。

重新在 ITDog 输入新 IP：
**国内全绿！国外全绿！**

打开终端，敲入：
`ssh -i ~/.ssh/id_rsa ubuntu@<新IP>`

`Welcome to Ubuntu 20.04.6 LTS!`

看着屏幕上重新出现的绿色终端提示符，窗外天边已经泛起了鱼肚白。这场历时数小时、跨越云端控制台、UEFI BIOS、单用户模式、磁盘离线挂载与 IP 替换的“救援战役”，终于画上了圆满的句号。
